spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/employeemanagementsystem?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=mysql

#can given your own port to avoid conflict with other application ports
server.port=4000

# Hibernate properties
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect (not working for latest spring 3.0 version)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# create, create-drop
spring.jpa.hibernate.ddl-auto=update
